# mamba env create -f environment.yml
# mamba activate proteindesign-dagster
# pip install -e .   # from this directory, installs the local pipeline package
name: proteindesign-dagster
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - dagster>=1.8.0
      - dagster-webserver>=1.8.0
      - pydantic>=2.0
